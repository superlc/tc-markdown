# Tasks: optimize-strikethrough-streaming

## Implementation Tasks

- [x] **补充删除线流式场景用例**
  - 在 Storybook 中加入删除线逐字输出验证内容（含闭合 `~~` 的 chunk 边界）

- [x] **优化行内预测策略（删除线）**
  - 当存在未闭合删除线且闭合标记被拆成单个 `~` 时，避免回退为纯文本导致闪现

- [x] **回归检查其他行内标记**
  - 确认粗体/斜体/行内代码/链接/图片的预测行为未被破坏

## Validation
- 删除线输入在流式追加过程中，视觉上**不出现纯文本 → 删除线**的回溯闪现。
- 在 chunk 边界落在 `~~` 前后时仍满足上述行为。
- 解析完成后最终 AST/HTML 与标准 GFM 一致。
